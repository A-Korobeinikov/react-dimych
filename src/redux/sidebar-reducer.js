let initialState = {
    friends: [
    { id: 1, name: "Dimych" },
    { id: 2, name: "Petya" },
    { id: 3, name: "Vasya" },
    ]
}

const sidebarReducer = (state = initialState, action) => {
    
    return state;
}

export default sidebarReducer;